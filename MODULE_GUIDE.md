# Chain 模块与修改指南

更新日期：2026-09-09。供后续开发者和 Codex 调整代码前阅读。当前为 C++20、每个程序单入口、头文件集中实现；函数沿用 PascalCase，数据流直接连接。

## 当前能力

账户余额与连续序号、明确的创世分配、可配置基础费销毁、并发验签、按账户组织交易池、原子状态落库、账户查询、钱包命令行工具、跟随节点重放与恢复均已实现。

候选排序保留“交易数多者优先；同数选择哈希小者”。同一候选的排名独立于重复提交它的身份数量。全网确认、分叉收敛和候选公平接收属于后续研究。日常测试使用单生产者和跟随节点。

当前数据库格式为 3，旧版为 2。升级使用新目录建立账户链，旧数据库保留。历史报告按原版本解释；当前性能见 `ACCOUNT_MODEL_REPORT_2026-09-09.md`。

## 模块地图

| 文件 | 职责 | 修改时关注 |
|---|---|---|
| `main.cpp` | 配置、创世配置加载、数据库初始化、线程与退出 | 启动恢复、退出顺序、默认目录 |
| `User/User.h` | `User{balance,nonce}`、创世摘要、账户转账规则 | 整数溢出、连续序号、自转、费用和确定性 |
| `Key/Key.h` | Ed25519 钱包生成与公私钥结构 | 密钥格式、私钥保护 |
| `Transaction/Transaction.h` | 交易编码、签名、交易池与序号索引 | 签名和状态检查的区别，两个索引和就绪计数 |
| `Transaction/Block.h` | 区块生成、内容验证、候选比较、提交后清池 | 同账户顺序、未来高度、正式确认状态 |
| `DB/DB.h` | RocksDB、账户缓存、临时执行、原子提交 | 锁顺序、磁盘与缓存发布时序、守恒 |
| `Network/Server.h` | 连接接收、报文长度、完整读取、验签投递、查询 | 超时、资源容量、协程边界 |
| `Network/Client.h` | 连接、发送队列、广播、同步、出块主循环 | 可执行池大小、每连接 strand、分叉 |
| `Message/Message.h` | 报文编码与长度约束 | 同时更新发送端、接收端和测试 |
| `Crypto/MerkleTree.h` | 有序交易哈希的默克尔树根 | 奇数叶重复、顺序影响树根 |
| `Crypto/VRF.h` | 可验证随机函数（Verifiable Random Function，VRF）实验 | 当前调用来自独立测试，选主接入另行设计 |
| `Tool/Tool.h` | 小端整数、公钥十六进制、哈希表散列 | 固定长度与非法输入 |
| `Time/Time.h` | 墙钟和单调时钟 | 性能使用单调时钟 |
| `Sender/Sender.cpp` | 钱包文件、签名数据、测试创世配置、发包和查询 | 同账户走同一连接，创世资金仅用于新链 |
| `Test/ChainTest.cpp` | 交易、存储、并发和密码学边界回归 | 保留原有问题覆盖 |
| `Test/UserTest.cpp` | 账户规则、失败原子性、恢复与序号池 | 状态规则变更优先补此处 |
| `Test/NetworkTest.py` | 分片报文、同步、重启和完整账户核对 | 使用新目录，核对链头与状态 |
| `Test/UserNetworkTest.py` | 钱包、转账、查询和带费用的复制 | 用户操作与网络入口 |
| `Test/Bench.cpp` | 签名、哈希、原始数据库写入微基准 | 原始数据库写入与账户执行分别解读 |
| `Test/RunBench.py` | 单节点、复制、延迟与资源采样 | 账户核对放在计时结束后 |
| `Test/CompareAccounts.py` | 同数据、同发送端、新旧交替对比 | 预热、重复与原始结果 |
| `scripts/Sync-Repo.ps1`、`.githooks/` | 日期命名、提交、推送和远端核对 | 仅暂存本任务文件，遵循 `AGENTS.md` |

`Test.cpp`、`Test/TestHash.cpp`、`Test/TxTest.h`、`ForEfficiency` 保留早期实验材料。正式回归入口是 `CMakeLists.txt` 中的四项 CTest。

## 主调用路径

### 接收交易

`Listen → AcceptPeer → session → ProcessMessage → ProcessTxPackage`

1. 网络协程先检查类型和长度，再完整读取正文。
2. `ReserveVerify` 预留容量，队列满时暂停连接读取，形成传输控制协议（Transmission Control Protocol，TCP）背压。
3. 独立线程池执行交易哈希和签名检查。
4. 数据库共享提交锁下读取内存余额和序号。新序号直接判定；过期序号按需查询历史交易哈希。
5. 交易池锁下检查重复、同发送者同序号冲突和容量，更新两个索引以及就绪数量。

### 生成与提交区块

`MainLoop → GenerateBlock → ProcessBlock → CommitBlock → DBCommitBlock`

`GenerateBlock` 按各账户已确认序号选取连续交易，预算使用已确认余额。选择后按序号、发送者公钥排序。保守预算使交易可独立排序；本地选块通常在后续区块使用刚收到的资金。

`VerifyBlock` 验证编码、哈希、交易签名、块内重复和树根。池内字节完全一致的交易可复用验签结果。

当前下一高度候选继续调用 `DBCheckBlockUsers`，在临时状态中执行所有交易，再参与候选比较。未来高度先缓存通过内容检查的区块，轮到它提交时使用实际父块状态执行账户规则。

`DBCommitBlock` 持有独占提交锁，重查父哈希与高度并执行账户规则。区块、交易、账户、销毁费用和链头放在一个批次写入。成功后发布内存账户变更，新增账户使用临时状态中的节点转移，减少写盘之后的内存分配。

`CommitBlock` 随后清理发送账户已确认的池内序号，包括与最终区块哈希不同的本地冲突交易。交易池接收阶段保持正式余额不变。

### 跟随同步

`QueryHeight → SyncBlock → QueryBlock → session → ProcessBlock → CommitBlock`

跟随节点使用相同账户执行函数，沿当前链头逐块重放。已保存账户在启动时加载。一个未知大块内部的签名验证仍顺序执行，是后续复制性能优化入口。

## 账户规则

| 规则 | 当前定义 |
|---|---|
| 金额 | 无符号 64 位整数最小单位，转账金额大于零 |
| 初始资金 | 创世配置分配，新公钥默认余额和序号均为零 |
| 初始总量 | 创世余额之和，求和检查溢出 |
| 确认序号 | 每账户单独记录，首笔为 1，之后严格加一 |
| 入池未来序号 | 最多领先已确认序号 4096，存在缺口时等待 |
| 余额 | 足够支付金额和基础费，用减法检查避免加法溢出 |
| 自转 | 金额受余额检查，最终只扣基础费，序号加一 |
| 收款 | 首次收款创建账户，累加检查溢出 |
| 基础费 | 创世配置 `base_fee`，默认零，确认时逐笔扣除并销毁 |
| 守恒 | 全部余额之和＋累计销毁费用＝创世总量 |
| 冲突入池 | 同发送者同序号保留先通过检查的一笔 |
| 多候选 | 分别用临时状态执行，正式状态在整块提交时生效 |

选块遇到序号连续但余额不足的交易时，移除该序号及后续池内交易，补充资金后可以重新提交。仅有序号缺口的交易继续等待。停机提交可执行交易，并允许尚有缺口的内存池正常退出；待处理池当前采用内存保存。

`sender --prepare` 输出的公钥分配文件用于创建新链。已有链账户状态来自已确认区块。

## 共享状态与锁顺序

| 数据 | 保护方式 |
|---|---|
| `users`、`userBurned`、链头和提交视图 | `dbCommitMutex`，读共享、提交独占 |
| `txpool`：哈希 → 完整交易 | `txpoolMutex` |
| `txUserPool`：发送者 → 有序序号/哈希与就绪序号 | 同一交易池锁 |
| `readyPoolTx`：连续序号前缀中的交易数 | 同一交易池锁 |
| `BlockBuffer`、`epoch` | `blockBufferLock`；`blockReady` 为原子标记 |
| `peerPool` | `peerMutex`，复制连接列表后投递发送 |
| 每连接的读写、关闭和发送队列 | 对应 Boost.Asio strand 串行执行 |

嵌套锁遵循 **`dbCommitMutex → txpoolMutex`**。候选缓存锁单独获取、尽快释放。正式账户与当前链头始终对应同一已发布提交；验签密集计算在这些锁外执行。

满块唤醒使用 `readyPoolTx`。总池数量包含未来序号，用总数触发满块会导致大量小块与同步写入。`PruneUserPool`、`DropUserPoolFrom`、入池代码应同时维护两个索引与就绪计数。

## 字节与存储格式

### 交易：176 字节

| 偏移 | 长度 | 内容 |
|---:|---:|---|
| 0 | 32 | 发送者公钥 |
| 32 | 32 | 收款者公钥 |
| 64 | 8 | 金额，小端 |
| 72 | 8 | 账户交易序号，小端 |
| 80 | 32 | 前 80 字节的 BLAKE2b 哈希 |
| 112 | 64 | 发送者对交易哈希的 Ed25519 签名 |

本次保留签名字节格式，使用同一份预签名数据对比性能。签名绑定链标识是后续协议升级任务；独立网络复用同一组资金公钥时需处理重放。

### 区块：112 字节头部＋交易

父哈希 32、树根 32、高度 8、交易数 8、前 80 字节哈希 32；最多 6000 笔交易。

创世块高度 1、交易数零，树根字段保存创世状态摘要：固定域字符串、基础费、账户数、按公钥排序的初始余额。普通块使用有序交易哈希的默克尔树根。

### 数据库格式 3

| 键 | 值 |
|---|---|
| `user/`＋32 字节公钥 | 16 字节：余额 8＋确认序号 8，小端 |
| `tx/`＋交易哈希 | 176 字节交易 |
| `block/`＋区块哈希 | 完整区块 |
| `height/`＋十进制高度 | 区块哈希 |
| `CurrentBlock` | 当前链头哈希 |
| `BlockHeight` | 十进制高度 |
| `GenesisConfig` | 规范化的公开创世配置 |
| `UserBurned` | 十进制累计销毁费用 |
| `SchemaVersion` | `3` |

启动加载账户表，检查值长度、供应总量、创世配置和链头。当前账户缓存是全量内存表，内存随账户数增长。原始 `DBWriteTx` 等辅助接口用于底层微基准，业务入口通过 `DBCommitBlock` 保存完整状态。

### 网络报文

握手为客户端类型 2，或节点类型 1 加两字节监听端口。后续消息由一字节类型、四字节正文长度、正文组成。

| 类型 | 正文 |
|---|---|
| 1 | 单笔 176 字节交易 |
| 10 | 多笔交易，最多 6000 笔 |
| 2、7 | 广播或同步区块 |
| 4 | 高度请求，空正文 |
| 5、12 | 八字节高度 |
| 6 | 请求区块的八字节高度 |
| 8、9 | 发现请求与六字节地址/端口列表 |
| 11 | 16 字节时间响应 |
| 13、14 | 统计请求和 JSON 结果 |
| 15 | 32 字节公钥，查询账户 |
| 16 | 存在标记 1＋余额 8＋序号 8 |

账户查询返回已确认状态。节点接入采用开放连接，接收成本、公平性与身份规则按研究目标继续定义。

## 验收与测试

执行 `ctest --test-dir build --output-on-failure`。账户验收包括：

- 创世分配、重复公钥、整数边界与供应总量。
- 零金额、余额不足、重复和跳跃序号、累计超支、自转费用。
- 乱序入池、连续前缀就绪、同序号冲突清理。
- 非法区块与写盘失败的状态原子性。
- 重启、错误创世配置、旧格式保护。
- 生产者和跟随者余额、序号与销毁费用一致。
- 钱包生成加载、签名转账和账户查询。

性能对比固定交易字节、发送端、线程、块大小、等待参数与同步写入。至少三次，报告中位数和范围。计时结束后独立执行数据集，并逐个查询账户核对。中央处理器（Central Processing Unit，CPU）占用依据进程 CPU 时间计算；不同阶段并行计时存在重叠。

## 后续任务及入口

1. **链标识签名绑定：** 修改 `SerializeTxForHash`、`GenerateTx`、`VerifyTransaction`、发送端和测试，制定明确的协议升级方案。
2. **确认与分叉收敛：** 从 `ProcessBlock`、`MainLoop`、`SyncBlock` 入手，定义周期、候选差异处理与确认条件，测试分区和重连。保留交易数优先作为研究对象。
3. **交易公平性：** 当前按账户连续前缀选交易，账户遍历顺序来自哈希表。测量高负载下各用户等待，再决定轮转或明确优先级。
4. **热点账户与未来序号：** 4096 是入池资源策略，研究按账户背压、重试与清晰的拒绝原因。
5. **复制性能：** 跟随节点大块验签可做有界分片；状态保持确定顺序，避免线程池同步等待自身子任务。
6. **缓存与长期运行：** 测量大量新账户时的内存、启动、数据库后台整理和磁盘空间。
7. **钱包与网络部署：** 当前钱包保存明文种子，按需要完善密钥存储、身份、传输保护和配额。
8. **多编译单元：** 当前全局实现由单入口包含，扩展多个源文件时统一处理全局符号。

后续更新同步修改本指南、`README.md` 和 `AGENTS.md` 的当前能力。固定日期的报告保留历史实验语义，新结果使用新日期记录。

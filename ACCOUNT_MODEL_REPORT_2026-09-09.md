# 账户模型、代码优化与性能对比

日期：2026-09-09。对比起点：`89108c2c98f6bbf4089f03f5648bf8b68ae1e022`。

本次完成账户余额、连续交易序号、创世资金分配、可配置基础费、钱包转账与查询，以及账户状态的保存和恢复。保留按交易数量、哈希比较候选区块的规则，继续沿用 PascalCase 函数命名、头文件组织和直接的数据流。

50 万笔固定交易、每组重复三次的测试中，8 线程吞吐量提高 **15.5%**，24 线程提高 **20.5%**。低负载延迟出现有升有降的结果，详见下表。

## 1. 实现了什么

| 部分 | 原版本 | 本版本 |
|---|---|---|
| 用户状态 | 交易携带金额和序号 | 每个公钥保存已确认余额与序号 |
| 资金来源 | 校验签名与交易结构 | 创世配置明确分配资金；新增公钥从零余额开始 |
| 交易执行 | 去重后保存交易 | 检查金额、余额、费用、连续序号，再更新收付款账户 |
| 多笔交易 | 缺少累计余额约束 | 在临时账户状态中按区块顺序执行，检查累计超支 |
| 交易池 | 按交易哈希保存 | 增加发送账户与有序序号索引，维护连续前缀数量 |
| 落库 | 区块、交易、链头批量保存 | 同一批次加入账户与累计费用，成功后发布内存状态 |
| 钱包操作 | 随机生成测试交易 | 生成和加载钱包、签名转账、查询余额与序号 |
| 重启与同步 | 恢复和复制区块 | 同时恢复和验证账户状态、资金总量与创世配置 |

账户结构为 `User{uint64_t balance, uint64_t nonce}`。金额采用整数最小单位，首笔序号为 1，确认后逐笔加一。自转只减少基础费并推进序号；普通转账增加收款余额并扣除发送金额和基础费。

基础费由创世配置 `base_fee` 决定，默认值为零。费用在确认时销毁，并记录累计数量。全部账户余额加累计销毁费用，始终等于创世资金总量。

## 2. 为什么速度提高

**新交易直接查询内存账户。** 已确认序号可以判断历史重放，新序号通常只需内存余额与序号检查。过期序号再按需查询历史哈希。区块提交时依靠连续序号执行排除重复，保留交易索引写入，减少逐笔数据库读取。

**交易池按账户组织。** 每账户维护有序序号，选块取连续、余额可支付的前缀。满块触发依据连续前缀中的交易数，避免未来序号堆积时频繁生成小块。发送端按公钥分配连接，同一账户保持发送顺序。

**每块合并账户写入。** 同一账户在一块内多次改变，只保存最终状态。临时执行表和正式缓存共享同一结构，提交后转移新增账户节点，减少内存分配。

本次比较反映“账户功能＋上述优化”的整体变化。两版都使用新的发送端，发送顺序和发包方式保持一致。账户功能的独立开销需要增加开关实验进一步分离。

## 3. 测试条件与口径

| 条件 | 设置 |
|---|---|
| 机器 | Intel Core i7-13700，16 个物理核心、24 个逻辑处理器，约 64 GiB 内存 |
| 系统与构建 | Windows 11，C++20，MinGW GCC 15.2，Release 构建 |
| 数据目录 | 两版均在 C 盘同一工作目录下建立全新数据库 |
| 存储 | HP EX900 固态硬盘（Solid-State Drive，SSD） |
| 交易 | 完全相同的 500,000 笔预签名交易，每笔 176 字节 |
| 账户 | 1024 个账户，每账户创世余额 1,000,000；环形转账，每笔金额 1 |
| 基础费 | 0；带费用的转账通过功能测试验证 |
| 并发 | 验签线程 8 或 24；网络线程 4；发送连接 8；每批 128 笔 |
| 区块 | 最多 6000 笔，等待目标 50 毫秒 |
| 持久化 | `--sync 1`，同步写入预写日志（Write-Ahead Log，WAL） |
| 重复 | 两版各先预热 50,000 笔，每组正式运行三次；第二轮交换先后顺序 |

每秒交易数（Transactions Per Second，TPS）使用 `local_commit_tps`：从节点接收首批交易，到最后一块本地提交完成。客户端起始到提交完成的速率另行记录。1024 个账户的独立余额、序号和总量核对在计时之后进行。

CPU 指中央处理器（Central Processing Unit）。总 CPU 占用以进程 CPU 时间除以测量时长和 24 个逻辑处理器计算。常驻内存（Resident Set Size，RSS）在测量期间采样，单位 MiB 为 1,048,576 字节。

## 4. 吞吐量与资源

### 三次运行的中位数

| 验签线程 | 原版本 TPS | 账户版 TPS | 变化 | 原版 CPU 占用 | 账户版 CPU 占用 | 原版峰值内存 | 账户版峰值内存 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 8 | 100,265 | 115,833 | +15.5% | 23.1% | 25.1% | 114.6 MiB | 106.9 MiB |
| 24 | 131,792 | 158,799 | +20.5% | 45.4% | 50.9% | 115.9 MiB | 113.6 MiB |

### 各轮 TPS

| 验签线程 | 版本 | 第 1 轮 | 第 2 轮 | 第 3 轮 |
|---:|---|---:|---:|---:|
| 8 | 原版本 | 100,265 | 97,744 | 100,504 |
| 8 | 账户版 | 116,265 | 115,833 | 101,368 |
| 24 | 原版本 | 136,987 | 131,792 | 122,654 |
| 24 | 账户版 | 160,935 | 158,799 | 158,226 |

包含客户端启动的吞吐量中位数：8 线程从 98,644 提高到 114,093；24 线程从 129,182 提高到 155,096。两种计时口径的方向一致。

全部 12 轮各完成 500,000 笔提交，结束时待验签数和交易池均为零。账户版每轮独立核对全部 1024 个账户，余额、序号、费用和供应总量一致。

8 线程账户版的第三轮低于前两轮，原始结果保留该波动。三轮短时实验适合版本回归比较，长期稳定上限需持续负载与不同账户分布验证。

### 阶段耗时：24 线程、每轮 50 万笔的累计毫秒中位数

| 指标 | 原版本 | 账户版 | 解释 |
|---|---:|---:|---|
| 验签工作线程 | 38,721.5 | 38,885.9 | 密码学工作量接近 |
| 入池状态读取 `tx_read_ms` | 1,810.7 | 58.8 | 内存账户判断减少历史数据库读取 |
| 等待提交锁 | 46,684.1 | 29,938.9 | 提交路径缩短后等待减少 |
| 等待交易池锁 | 772.5 | 3,145.2 | 有序索引维护增加锁内工作 |
| 区块生成 | 401.2 | 416.1 | 增加连续序号与余额预算处理 |
| 整块提交 | 2,287.8 | 1,592.5 | 包含状态处理与数据库写入 |
| 数据库写入 | 1,048.4 | 1,398.2 | 新增账户数据后，此阶段约增加 33.4% |
| 账户临时执行 | — | 42.3 | 包含候选检查与提交复查 |

阶段时间包含嵌套及多个线程的累计值，具有重叠，分别用于寻找成本来源。24 线程下，每轮进程 CPU 时间中位数从 43.33 秒降到 39.39 秒；任务更快结束，因此总 CPU 占用比例上升。

当前继续提速应优先看验签、同步写入和锁等待。24 线程总 CPU 占用约 51%，提交与磁盘写入占据串行路径。增加账户后，数据库写入和池锁等待有所增加；内存查询优化带来的减少更大。这些计时支持上述定位，后续可通过采样分析和存储参数对照进一步拆分。

## 5. 延迟变化

逐笔发送不同交易，查询确认结果后再发送下一笔，每种配置各 100 个样本。耗时包含建立传输控制协议（Transmission Control Protocol，TCP）连接、发包、等待出块、落库与查询轮询。

P50、P95、P99 分别表示第 50、95、99 百分位延迟（Percentile Latency）。

| 等待目标 | 指标 | 原版本 | 账户版 | 变化 |
|---:|---|---:|---:|---:|
| 10 毫秒 | P50 | 16.52 毫秒 | 23.40 毫秒 | +6.88 毫秒，约 +41.7% |
| 10 毫秒 | P95 | 46.29 毫秒 | 39.76 毫秒 | −6.53 毫秒，约 −14.1% |
| 10 毫秒 | P99 | 47.91 毫秒 | 46.64 毫秒 | −1.27 毫秒，约 −2.6% |
| 50 毫秒 | P50 | 76.64 毫秒 | 75.55 毫秒 | −1.10 毫秒，约 −1.4% |
| 50 毫秒 | P95 | 80.53 毫秒 | 92.44 毫秒 | +11.91 毫秒，约 +14.8% |
| 50 毫秒 | P99 | 94.60 毫秒 | 94.93 毫秒 | +0.33 毫秒，约 +0.3% |

本次观察到 10 毫秒配置的中位延迟上升、50 毫秒配置的 P95 上升。样本集中在约 16、31、63、79 毫秒附近，系统调度、连接与查询周期均影响结果。当前 100 样本尚不足以把这些变化单独归因于账户计算；下一步使用持续连接、增加样本并记录节点内部时间点进行确认。

## 6. 正确性检查

四项自动测试全部通过：

| 测试 | 覆盖 |
|---|---|
| `ChainCorrectness` | 2239 项断言，覆盖交易、密码学、存储与并发回归 |
| `AccountCorrectness` | 53 项检查，覆盖创世资金、费用、整数边界、序号、累计超支、状态原子性和恢复 |
| `ChainNetwork` | 10 个场景，覆盖完整读取、报文限制、同步、重启和全部账户核对 |
| `AccountNetwork` | 7 个场景，覆盖钱包、转账、查询、自转费用、无资金账户、复制与恢复 |

新增重点检查：乱序交易填补缺口后变为可执行；同账户同序号冲突只保留一笔；超支区块整块拒绝；本地超支后缀从两个池索引及就绪计数一起移除；外部区块提交后清理本地冲突候选；只读数据库写入失败时余额与费用保持原状态；旧格式标记保留；退出允许池内存在序号缺口。

## 7. 升级与继续开发

数据库采用 `SchemaVersion=3`。使用新的 `--data` 目录和明确的 `--genesis` 配置启动账户链，原数据库保留。已有账户库重启时读取已保存的创世配置；显式配置必须与其一致。

运行步骤见 [README.md](README.md)。各模块职责、调用链、锁顺序、协议字节、数据库键和修改注意事项集中在 [MODULE_GUIDE.md](MODULE_GUIDE.md)，后续 Codex 按 [AGENTS.md](AGENTS.md) 的工作规则读取。

后续优先事项：

1. 将链标识绑定到交易签名，明确跨链重放与升级方案。
2. 定义多生产者确认、分叉收敛、周期与候选公平接收规则，并增加分区重连测试。
3. 测量热点账户、持续连接延迟、多机复制以及大量新账户时的内存与数据库表现。

当前交易池允许序号领先 4096；选块预算基于已确认余额；账户缓存全量保存在内存。账户与费用为交易提供资金约束，开放提议环境的资源成本和候选选择仍需配套设计。本次吞吐量对应单机验证、账户执行和持久化，网络最终确认性能随后续确认机制单独评估。

## 8. 可复核材料

- [原始性能结果](ACCOUNT_PERFORMANCE_RESULTS_2026-09-09.json)：12 轮吞吐量、4 组延迟原始样本、资源和账户核对结果。
- [功能测试记录](ACCOUNT_TEST_RESULTS_2026-09-09.txt)：测试入口、通过数量与耗时。
- `Test/CompareAccounts.py`：相同签名数据和发送端的新旧交替测试。
- `Test/RunBench.py`：吞吐量、复制、延迟和资源采样。

性能文件包含数据、创世配置、发送端和两版节点的 SHA-256（Secure Hash Algorithm 256-bit，256 位安全散列算法）摘要，用于确认实验输入和程序一致。正式测量后补充了超支后缀回归测试、代码注释和文档，节点执行逻辑保持一致。

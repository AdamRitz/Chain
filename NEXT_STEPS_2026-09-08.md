# Chain 下一步任务（2026-09-08 修复后更新）

本清单替代修复前的任务顺序。历史问题及其证据保留在 `CODE_REVIEW_2026-09-08.md`；最新完成项和实测性能见 `OPTIMIZATION_REPORT_2026-09-08.md`。

## 本次已完成

- 交易内容重新计算 hash 后验签，区块内每笔交易验证、同块去重和已确认交易防重放。
- 修正批量交易偏移、报文长度、截断读取、超大分配和同步返回内容。
- I/O 与 CPU 验签分离，短交易池锁、并发数据库读取、有限验证队列与 TCP 背压。
- 广播确实被调度；每个连接只有一条串行发送队列；接受连接不被慢握手阻塞。
- 条件变量驱动出块，保留未提交交易，写库失败不推进高度或删除交易。
- 区块、交易、索引和链头一次 WriteBatch 原子写入，默认同步 WAL；恢复现有链头。
- 核心回归、进程级网络测试、可复现压测、日期命名和 GitHub 自动推送。

## P0：先成为能正确转账的账本

1. **账户状态。** 增加 `Account{balance,nonce}`、确定的创世分配、交易 chain ID/domain、余额足够及 nonce 连续检查、收款余额溢出检查。签名有效不代表账户有钱；当前 nonce 只是签名中的字段。
2. **原子执行。** 一个区块先在临时状态上顺序执行，整块有效后将变更账户与区块放入同一个 WriteBatch。验证其他节点区块必须走相同执行函数。不要在 mempool 接收时直接改正式余额。
3. **确定性和恢复。** 明确交易排序、余额查询、重放/双花规则，增加失败块无副作用、重启后状态一致、跨链重放、同发送者 nonce 冲突测试。不要把当前按 hash 排序直接当成合法 nonce 顺序。

建议第一阶段账户模型先做确定性的顺序状态执行；当前验签已经并行。下一阶段按实际读写账户集合分组并行执行，包含收款账户冲突；同一发送者和热点账户必须有明确顺序。

## P0：明确网络信任模型

- 当前只支持实验性的单生产者与跟随节点。要多生产者容错，先选定权限节点集合和最终性要求，再实现 leader/term、投票/仲裁证明、超时换主、持久化投票及故障恢复。
- 可信集群需要崩溃容错时可评估 Raft；存在恶意验证者时必须采用相应 BFT 协议。不能把候选块“交易多、hash 小优先”的规则当作共识。
- 加入握手版本、chain ID、节点身份及请求权限，限制历史查询和畸形请求频率。实现断线重连与分叉检测；目前历史同步不支持回滚重组。

## P1：继续优化，有结果再增加复杂度

- **复制链路优先。** 本次两节点确认明显低于生产者本地提交。跟随者当前对一个未知大块逐笔验签；先实现出块前交易批量传播（去重、跳过来源 peer、保留背压），利用现有验签池；再评估块内分片验签与有界完成聚合，禁止在线程池中同步等待自身子任务。

- 用相同笔数、相同落盘语义的长期测试测出块提交锁、DB 查询和 RocksDB compaction。把数据从项目 D 盘移到 SSD 时，用 `--data` 指向 SSD 目录即可，代码位置无需改变。
- 增加真实账户状态读写后重测；当前 TPS 不包括余额执行，不能沿用为完整账本性能承诺。
- 增加两台物理机器的吞吐、P95/P99、慢节点和网络丢包测试，再定义共识确认指标。
- 对热点交易做冲突批次与顺序队列；只有池锁等待确实占主导后再考虑分片池。避免先引入无锁容器、Actor 或通用调度框架。
- 更大数据集验证缓存失效、SST 层数、写停顿、磁盘空间和日常后台负载影响；短时峰值不能替代持续 TPS。

## P2：维护和密码学

- 钱包持久化与安全加载；当前启动钱包仍是临时生成。实验 VRF 需要标准实现和测试向量；本轮边界检查不等于密码学安全证明。
- 多翻译单元开发时再处理头文件全局定义的 ODR 问题；现阶段各可执行文件各自单个入口，保留现有组织习惯。
- CI 中固定编译器、依赖及 Python 版本；跨平台、模糊测试、长时间并发测试和断电/故障注入逐项增加。

## 下一次推荐交付

先实现最小账户账本和配套测试，继续使用现有 `Transaction/`、`DB/`、`Key/` 目录及 PascalCase 函数。预计新增/修改生产代码约 **600–1000 行**、测试约 **400–700 行**，合计 **1000–1700 行**；不包含共识、智能合约、GUI 或通用 VM。验收标准应为“余额守恒、nonce 正确、重放和溢出拒绝、整块原子提交、重启一致”，然后再验收 TPS。

# Chain 下一步任务与验收清单

日期：2026-09-08。适用工作区：`D:\Project\Chain`。本文件是待执行计划，不代表以下修复已经完成。问题编号对应 `CODE_REVIEW_2026-09-08.md`。

建议按 M0 → M1 → M2 → M3 执行。性能指标要从 M0 就开始记录，但吞吐优化应在对应正确性和恢复测试通过后推进。

## M0：修复验证与内存安全，建立回归门槛

### T01 / P0：统一交易与区块验证（R01、R02）

- [ ] 重算交易规范内容的 Hash 并比较，再进行验签；固定编码端序，明确链 ID、版本和签名用途。
- [ ] 区块中任意交易验证失败立即拒绝；禁止以日志替代返回失败。
- [ ] 修复批量交易 offset，拒绝非整笔长度及超额包；单笔、批量、提议与同步使用同一验证逻辑。
- [ ] 设计已验证交易的表示类型，避免原始网络数据被误标记为可信交易。

验收：合法交易可通过；修改 sender/receiver/amount/nonce/hash/signature 的负例被拒绝；两笔不同交易都处理；无效交易不能由其他入口绕过；区块 Merkle/Hash 不符被拒绝。

### T02 / P0：统一有界协议解析（R03、R04、R05）

- [ ] 建立唯一帧编码器/解析器，长度只表示载荷长度；写清版本、端序、消息类型与载荷格式。
- [ ] 修复 Peer key 指针运算、BlockBuffer 索引、发现/高度消息容量及缺失 return。
- [ ] 所有固定字段读前检查大小；按除法先校验交易数，防止长度乘法溢出。
- [ ] 消息体读取失败不得继续分派；设置帧大小、交易包数、连接数及读取时限。
- [ ] 给所有消息写往返、半包、粘包、截断、未知类型及极值长度测试。

验收：不可信长度不会导致任意分配；畸形帧被稳定拒绝；每个合法帧可被下一帧连续解析；在可用工具链下运行 ASan/UBSan 或等价检查，发现的问题修至通过。

### T03 / P1：建立可重复构建与测试入口

- [ ] 添加 `enable_testing/add_test`，将本次复现变成“修复后应拒绝”的自动断言。
- [ ] 将 test 示例与真正链行为测试区分；给 sender/crypto 基准明确入口及用途。
- [ ] 用 CMake presets/toolchain 或包管理配置替代硬编码机器路径，明确依赖版本。
- [ ] 修复已知 warning，至少让缺失 return、忽略异步结果、越界警告成为失败门槛。
- [ ] 数据目录和 config 路径改为显式参数；测试总是使用独立临时目录。

验收：干净构建目录能配置和构建；CTest 能列出并执行真实测试；在不打开原数据库的情况下运行全部测试。

## M1：打通可恢复的节点流程

### T04 / P1：修复协程与 Peer 生命周期（R06、R07）

依赖：T02。

- [ ] 区分同步发送入队函数与真正协程；所有异步操作显式等待或启动，并处理异常。
- [ ] 修复 BroadcastData、ProcessDiscoverMessage、deferred co_spawn 的调度。
- [ ] 不持有普通 mutex 跨 co_await；Connect 与 AddPeer 避免嵌套同锁。
- [ ] 主动和被动连接都启动读循环；握手单独调度并设置截止时间。
- [ ] 使用同一个 Peer strand 管理读、写、关闭、计时字段和发送队列；限制队列字节数。
- [ ] 接入可配置 seed 和重连，修复发现阈值的逻辑非运算优先级问题。

验收：两个本地独立节点可双向收发并确认收到同一交易/块；同 IP 不同端口可共存；慢握手不阻塞其他连接；断线重连没有死锁、空指针或持续增长的队列。

### T05 / P0：实现启动恢复与原子提交（R08、R09）

依赖：T01、T02。

- [ ] 仅新数据库生成创世块；恢复现有 CurrentBlock/height，并检查索引与块体一致。
- [ ] 所有 RocksDB 状态显式检查并向上传递；高度解析支持 uint64_t 和错误处理。
- [ ] 按高度读取完成 `height → hash → block` 两次查找。
- [ ] 采用有前缀或列族的键空间，例如 tx/、block/、height/、meta/、state/。
- [ ] 每次提交用 WriteBatch 包含块、索引、账本状态和链头；明确 WAL 及同步策略。
- [ ] 提议中的交易保持 reserved 状态；提交成功后才删除；失败/换轮能够释放。

验收：重启前后高度和链头保持；区块与索引匹配；模拟存储失败不推进链头、不丢交易；故障后只能看到完整提交前或完整提交后的状态；在约定持久化模型下验证已确认数据恢复。

### T06 / P1：集中链状态，补上交易执行（R07、R12）

依赖：T01、T05。

- [ ] 用链状态 strand 或单线程事件循环独占 epoch、候选块和账本状态。
- [ ] 明确账户或 UTXO 模型、创世分配、amount 规则、nonce/重放语义。
- [ ] 按确定顺序执行余额变化，检查不足、整数溢出、同账户冲突及重复交易。
- [ ] 增加状态根/执行结果承诺和一致的验证规则。
- [ ] 高度只在成功提交后推进；空交易池不能阻断收到的合法提议与共识消息。

验收：相同初始状态和交易顺序得到相同状态根；跨节点结果一致；重复 nonce、双花、余额不足被按规则处理；并发输入不改变执行结果。

### T07 / P1：完成经过验证的历史同步（R05、R08）

依赖：T04、T05、T06。

- [ ] 明确高度请求、区块请求、响应和错误格式，增加请求关联及超时重试。
- [ ] 选择实际存在的 Peer，不能用 `peerPool[0]` 作为默认节点。
- [ ] 按连续高度验证前块、交易和状态，再通过正常提交路径写库。
- [ ] 限制未来块窗口、并发请求及响应字节数；拒绝未知/不匹配响应。

验收：落后节点追上链头且状态根一致；恶意或损坏区块不会直接写入链；失败请求可重试而不会跳过高度。

## M2：明确并实现最终性

### T08 / P1：先决定信任和故障模型（R10）

- [ ] 写明是否允许恶意验证者、准入方式、验证者数量/权重、网络环境和确认承诺。
- [ ] 仅崩溃故障可评估成熟 Raft；需要 BFT 则采用完整且有依据的协议/实现。
- [ ] 定义 received、validated、proposed、committed 的状态与查询/订阅 API。
- [ ] 把共振调整从提交条件中分离；时钟只用于调度、观测与超时。

验收：形成协议说明和可执行的状态转换测试；业务接口不会把“本地收到”或“本地打包”误报为最终确认。

### T09 / P1：实现协议与故障测试

依赖：T04–T08。若选用成熟共识引擎，本项重点是正确集成及测试，而不是重复实现其内部协议。

- [ ] 若走 BFT：固定验证者、提议签名、height/round、投票权、锁定/换锁、法定人数证明、超时换轮、持久防双签。
- [ ] 共识证明满足后执行规定的提交顺序并通知客户端；保留证明供同步验证。
- [ ] 测试正常路径、掉线提议者、延迟/乱序、重复投票、无效块、重启和网络分区。
- [ ] 4 个等权 BFT 验证者下验证 1 个故障仍可推进；2–2 分区时不能在双方产生冲突的最终提交，恢复后按协议继续。

验收：在所声明故障模型内，各正确节点不会最终提交同高度的不同块；符合活性前提时能够恢复推进。单元测试通过不能代替正式的协议安全依据。

## M3：基于测量降低端到端延迟

### T10 / P1：计时、观测与可控负载（R11）

- [ ] 用 txid/请求 ID 关联入口、验证、提议、投票、落库和通知。
- [ ] 计时使用单调时钟；跨机时间戳统一单位并与 RTT/时钟偏移分开。
- [ ] sender 支持固定速率、并发与突发模型，接收真实状态回执。
- [ ] 记录 P50/P95/P99、最终提交 TPS、错误率、队列年龄、CPU/内存、网络及存储指标。

验收：能够解释一笔慢交易主要等在哪一步；每次性能结论附机器、网络、持久化和负载配置。

### T11 / P2：先减少等待、排队和写放大

依赖：T09、T10；局部基准可更早进行，但不得声称测得最终确认性能。

- [ ] 使用事件唤醒/steady_timer，移除忙等、10ms 轮询和空池固定 sleep。
- [ ] 测试 10/25/50/100ms 最大批等待及不同字节上限，观察换轮率和确认尾延迟。
- [ ] 用有界 CPU 验证池，密码学验证放在锁外，账本提交保持确定顺序。
- [ ] 对广播使用共享不可变缓冲区，避免每 Peer 重复复制大块；保持异步缓冲区生命周期正确。
- [ ] 比较增量交易传播与完整块传播；为投票/控制消息设置资源预算，防止被大块排队拖住。
- [ ] 比较 WriteBatch 改造前后延迟，始终保持相同耐久级别；记录 compaction 的影响。

验收：低负载首笔交易消除固定半秒级等待；超载时资源保持有界并明确拒绝；优化后的 P99 和错误率优于同条件基线，所有正确性、恢复及一致性测试仍通过。

### T12 / P2：工程整理与可选 VRF 后续（R13）

- [ ] 拆分 Network、Codec、Mempool、Ledger、Consensus、Storage、Metrics 的职责与头文件定义。
- [ ] 使用 RAII 管理线程退出、Remotery 和数据库，配置至少一个 I/O 工作线程，处理初始化失败。
- [ ] 钱包/验证者身份持久化并设计明确的密钥管理方式；清理测试数据与构建产物的版本管理规则。
- [ ] 只有协议确实需要时再推进 VRF：采用标准套件、长度/点/标量验证、错误处理和官方测试向量。

验收：模块可独立测试；退出和启动路径可重复；VRF 的测试和用途不再与共识完整性混淆。

## 本轮最小可交付范围

优先完成 T01、T02、T03、T04、T05，形成可编译、可回归验证、双节点可传播、存储可恢复的版本。随后推进账本状态和共识。暂不把自写容器、更换 Hash、DAG、分片或降低签名验证强度作为近期工作。
